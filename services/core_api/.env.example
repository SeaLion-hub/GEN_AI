# .env.example (services/core_api/.env.example)

# --- B님 (AI 서비스) 용 ---
# (B님의 실제 키가 아닌, A님이 자신의 키를 넣도록 알려주는 역할)
OPENAI_API_KEY="YOUR_OPENAI_KEY_HERE"

# --- A님 (DB / 인증) 용 ---
# (A님이 자신의 로컬 DB 주소를 입력하도록 알려주는 역할)
DATABASE_URL="postgresql://USERNAME:PASSWORD@localhost:5432/reflex_db"

# (A님이 자신의 비밀 키를 입력하도록 알려주는 역할)
SECRET_KEY="YOUR_SUPER_STRONG_SECRET_KEY_HERE"